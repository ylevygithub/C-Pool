/*
We’ve discussed how you had to explicitly specify the type parameter
when calling a function template.
However, the compiler may automatically deduce the template parameters.
For instance:
In my main function,
I've removed the explicit type specifications when calling add.
This lets my compiler automatically deduce the template parameters.
*/

