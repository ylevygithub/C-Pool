//
// EPITECH PROJECT, 2020
// c++pool
// File description:
// Parts.cpp
//

#include "Parts.hpp"

bool isFunctional()
{
    if (isFunctional() == true)
        return (true);
    else if (isFunctional() == false)
        return (false);
}

std::string print_ok()
{
    if (isFunctional() == true)
        return "OK";
    else
        return "KO";
}